#import "meta/meta.graphql"

query widgetArea(
  $apiKey: String!
  $family: String = null
  $areaName: String = null
  $alias: String = null
  $displaySetting: String = null
  $filters: [PageWidgetCollectionFilterInputType] = null
) {
  widgetArea(
    apiKey: $apiKey
    family: $family
    areaName: $areaName
    alias: $alias
    displaySetting: $displaySetting
    filters: $filters
  ) {
    meta {
      ...Meta
    }
    containers {
      layout
      design
      widgets {
        name
        configuration
        images {
          fileName
          largestSize {
            imageWidth
            imageHeight
          }
        }
      }
    }
  }
}
