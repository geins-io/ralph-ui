#import "product/price.graphql"
query products($filter: FilterInputType = null) {
  products(filter: $filter) {
    products {
      brand {
        name
        alias
        canonicalUrl
      }
      categories {
        name
        alias
        canonicalUrl
      }
      name
      productId
      alias
      canonicalUrl
      unitPrice {
        ...Price
      }
      images
    }
    count
  }
}
