#import "global/menu-item-base.graphql"
query getMenuAtLocation($menuLocationId: String, $channelId: String) {
  getMenuAtLocation(menuLocationId: $menuLocationId, channelId: $channelId) {
    title
    menuItems {
      ...MenuItemBase
    }
  }
}
