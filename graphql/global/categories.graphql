query categories($channelId: String, $languageId: String) {
  categories(channelId: $channelId, languageId: $languageId) {
    alias
    canonicalUrl
    name
    categoryId
    parentCategoryId
    order
  }
}
