query categories($channelId: String, $languageId: String, $marketId: String) {
  categories(
    channelId: $channelId
    languageId: $languageId
    marketId: $marketId
  ) {
    alias
    canonicalUrl
    name
    categoryId
    parentCategoryId
    order
  }
}
