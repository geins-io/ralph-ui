mutation subscribeToNewsletter(
  $email: String!
  $channelId: String
  $languageId: String
) {
  subscribeToNewsletter(
    email: $email
    channelId: $channelId
    languageId: $languageId
  )
}
