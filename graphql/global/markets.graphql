query channel($channelId: String) {
  channel(channelId: $channelId) {
    defaultMarketId
    markets {
      id
      defaultLanguageId
      alias
      country {
        name
        code
      }
    }
  }
}
