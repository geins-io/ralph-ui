#import "productlist/list-info.graphql"

query listPageInfo(
  $url: String!
  $channelId: String
  $languageId: String
  $marketId: String
) {
  listPageInfo(
    url: $url
    channelId: $channelId
    languageId: $languageId
    marketId: $marketId
  ) {
    ...ListInfo
  }
}
