#import "product/price.graphql"

fragment ListProduct on ProductType {
  brand {
    name
  }
  name
  productId
  alias
  canonicalUrl
  unitPrice {
    ...Price
  }
  images
  discountCampaigns {
    hideTitle
    name
  }
}
