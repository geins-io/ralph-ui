#import "productlist/product.graphql"

query products(
  $skip: Int = null
  $take: Int = null
  $filter: FilterInputType = null
  $channelId: String
) {
  products(skip: $skip, take: $take, filter: $filter, channelId: $channelId) {
    products {
      ...ListProduct
    }
    count
  }
}
