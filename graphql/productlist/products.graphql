#import "productlist/product.graphql"

query products(
  $skip: Int = null
  $take: Int = null
  $filter: FilterInputType = null
) {
  products(skip: $skip, take: $take, filter: $filter) {
    products {
      ...ListProduct
    }
    count
  }
}
