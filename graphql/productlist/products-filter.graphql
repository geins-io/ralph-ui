#import "productlist/filters.graphql"

query products($categoryAlias: String = null, $brandAlias: String = null) {
  products(categoryAlias: $categoryAlias, brandAlias: $brandAlias) {
    filters {
      ...ListFilters
    }
  }
}
