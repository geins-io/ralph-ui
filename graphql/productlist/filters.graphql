fragment ListFilters on FilterCollectionType {
  facets {
    filterId
    group
    label
    type
    values {
      _id
      count
      facetId
      parentId
      label
      order
      hidden
    }
  }
}
