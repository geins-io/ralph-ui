#import "productlist/list-info.graphql"

query listPageInfo($alias: String!) {
  listPageInfo(alias: $alias) {
    subCategories {
      name
      alias
      canonicalUrl
    }
    ...ListInfo
  }
}
