query listPageInfo($apiKey: String!, $categoryAlias: String!) {
  listPageInfo(apiKey: $apiKey, categoryAlias: $categoryAlias) {
    alias
    primaryImage
    name
    categoryId
    primaryDescription
    secondaryDescription
    subCategories {
      alias
      name
    }
  }
}
