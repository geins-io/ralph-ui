#import "productlist/list-info.graphql"

query listPageInfo($alias: String!, $channelId: String) {
  listPageInfo(alias: $alias, channelId: $channelId) {
    subCategories {
      name
      alias
      canonicalUrl
    }
    ...ListInfo
  }
}
