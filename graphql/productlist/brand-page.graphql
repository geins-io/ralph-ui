#import "meta/meta.graphql"

query listPageInfo($apiKey: String!, $alias: String!) {
  listPageInfo(apiKey: $apiKey, alias: $alias) {
    alias
    primaryImage
    logo
    name
    brandId
    primaryDescription
    secondaryDescription
    meta {
      ...Meta
    }
  }
}
