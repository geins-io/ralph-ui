#import "productlist/list-info.graphql"

query listPageInfo($alias: String!, $channelId: String, $languageId: String) {
  listPageInfo(alias: $alias, channelId: $channelId, languageId: $languageId) {
    logo
    ...ListInfo
  }
}
