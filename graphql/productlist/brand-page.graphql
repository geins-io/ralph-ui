#import "meta/meta.graphql"

query listPageInfo($alias: String!) {
  listPageInfo(alias: $alias) {
    alias
    primaryImage
    logo
    name
    id
    primaryDescription
    secondaryDescription
    meta {
      ...Meta
    }
  }
}
