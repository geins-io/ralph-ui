query listPageInfo($apiKey: String!, $brandAlias: String!) {
  listPageInfo(apiKey: $apiKey, brandAlias: $brandAlias) {
    alias
    primaryImage
    logo
    name
    brandId
    primaryDescription
    secondaryDescription
  }
}
