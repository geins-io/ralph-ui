#import "cart/content.graphql"

mutation addToCart(
  $id: String!
  $item: CartItemInputType!
  $channelId: String
  $languageId: String
) {
  addToCart(
    id: $id
    item: $item
    channelId: $channelId
    languageId: $languageId
  ) {
    ...Cart
  }
}
