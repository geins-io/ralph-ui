#import "cart/content.graphql"

mutation setCartPromoCode(
  $id: String!
  $promoCode: String!
  $channelId: String
  $languageId: String
) {
  setCartPromoCode(
    id: $id
    promoCode: $promoCode
    channelId: $channelId
    languageId: $languageId
  ) {
    ...Cart
  }
}
