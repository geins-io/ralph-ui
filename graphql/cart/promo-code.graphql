#import "cart/content.graphql"

mutation setCartPromoCode(
  $id: String!
  $promoCode: String!
  $channelId: String
) {
  setCartPromoCode(id: $id, promoCode: $promoCode, channelId: $channelId) {
    ...Cart
  }
}
