#import "cart/content.graphql"

mutation completeCart($id: String!, $channelId: String, $languageId: String) {
  completeCart(id: $id, channelId: $channelId, languageId: $languageId) {
    isCompleted
    ...Cart
  }
}
