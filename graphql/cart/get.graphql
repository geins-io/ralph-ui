#import "cart/content.graphql"

query getCart(
  $id: String!
  $channelId: String
  $languageId: String
  $marketId: String
) {
  getCart(
    id: $id
    channelId: $channelId
    languageId: $languageId
    marketId: $marketId
  ) {
    ...Cart
  }
}
