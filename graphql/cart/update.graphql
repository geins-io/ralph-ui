#import "cart/content.graphql"

mutation updateCartItem(
  $id: String!
  $item: CartItemInputType!
  $channelId: String
) {
  updateCartItem(id: $id, item: $item, channelId: $channelId) {
    ...Cart
  }
}
