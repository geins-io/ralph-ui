#import "cart/content.graphql"

query getCart($id: String!, $channelId: String, $languageId: String) {
  getCart(
    id: $id
    includeCompleted: true
    channelId: $channelId
    languageId: $languageId
  ) {
    isCompleted
    ...Cart
  }
}
