#import "product/sku.graphql"
#import "product/price.graphql"

query relatedProducts(
  $prodAlias: String!
  $channelId: String
  $languageId: String
) {
  relatedProducts(
    alias: $prodAlias
    channelId: $channelId
    languageId: $languageId
  ) {
    alias
    name
    brand {
      name
    }
    canonicalUrl
    unitPrice {
      ...Price
    }
    relation
    images
    skus {
      ...Sku
    }
  }
}
