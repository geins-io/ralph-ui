#import "product/price.graphql"
#import "product/variant-base.graphql"

query product($alias: String!, $apiKey: String!) {
  product(alias: $alias, apiKey: $apiKey) {
    productId
    name
    brand {
      name
      alias
    }
    images
    categories {
      name
    }
    unitPrice {
      ...Price
    }
    shortText
    currentProductVariant {
      ...VariantBase
    }
    variantGroups {
      variantLevels {
        name
        type
        level
        values {
          label
          value
        }
      }
      variants {
        ...VariantBase
      }
    }
  }
}
