mutation placeOrder(
  $cartId: String!
  $checkout: CheckoutInputType!
  $channelId: String
) {
  placeOrder(cartId: $cartId, checkout: $checkout, channelId: $channelId) {
    orderId
    redirectUrl
    status
  }
}
