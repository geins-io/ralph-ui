query getCheckoutAndOrder(
  $id: String!
  $paymentType: PaymentType!
  $channelId: String
  $languageId: String
  $marketId: String
) {
  getCheckoutAndOrder(
    id: $id
    paymentType: $paymentType
    channelId: $channelId
    languageId: $languageId
    marketId: $marketId
  ) {
    order {
      orderId
      firstName
      lastName
      email
      currency
      marketId
    }
  }
}
