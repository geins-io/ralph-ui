#import "user/user.graphql"
mutation updateUser(
  $user: UserInputType!
  $channelId: String
  $languageId: String
) {
  updateUser(user: $user, channelId: $channelId, languageId: $languageId) {
    ...User
  }
}
