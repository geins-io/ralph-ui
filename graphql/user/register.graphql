#import "user/jwt-token.graphql"

mutation registerUser(
  $apiKey: String!
  $username: String!
  $password: String!
) {
  registerUser(apiKey: $apiKey, username: $username, password: $password) {
    ...JwtToken
  }
}
