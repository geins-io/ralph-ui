mutation commitReset(
  $resetKey: String!
  $password: String!
  $channelId: String
  $languageId: String
) {
  commitReset(
    resetKey: $resetKey
    password: $password
    channelId: $channelId
    languageId: $languageId
  )
}
